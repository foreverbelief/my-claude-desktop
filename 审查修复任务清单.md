# MY-CODE 项目审查修复任务清单

> 生成日期：2026-08-17
> 项目：`G:\桌面\论文相关\操作面板\code\tokenicode-deepseek-alpha`（Tauri 2 + React 19，Claude Code GUI 客户端 v1.0.6）
> 状态标记：⬜ 待修复 / 🔧 修复中 / ✅ 已修复 / ⏭️ 已评估不修（附原因）
> 最终状态：2026-08-17 全部完成；前端 `pnpm build` ✓（tsc+vite）、后端 `cargo check/build` ✓、`pnpm tauri dev` 启动测试 ✓（应用窗口进程正常，Vite 1420 就绪，CLI 解析正常）

---

## 一、严重（影响安全与核心功能）

- [x] **S1. 默认模式为 bypass（全自动危险模式）**
  - 位置：`src/stores/settingsStore.ts`、`src/lib/i18n.ts`、`src/components/chat/ModeSelector.tsx`
  - ✅ 修复：
    1. 默认 `sessionMode` 从 `'bypass'` 改为 `'code'`（`settingsStore.ts:201`）
    2. migration v5 不再强制 bypass，已持久化的 bypass 降级为 code（`:392-395`）
    3. i18n 标签「全自动」改为「全自动（跳过确认）」/「Full Auto (skip all confirmations)」
    4. 切换到 bypass 需二次确认弹窗（`ModeSelector.tsx` 新增 `pendingBypass` + ConfirmDialog，新增 `mode.bypassConfirm*` 三个 i18n key）

- [ ] **S2. 任意文件读写——无项目根白名单**
  - 位置：`src-tauri/src/lib.rs:3454-3467`（reject_unsafe_path）、`:3470-3568`（read/write/delete/copy/rename/create）、`export_session_markdown`/`write_skill`/`delete_skill`
  - ⏭️ 已评估不修：`reject_unsafe_path` 的完整白名单需要把"活动项目根"从会话管道进 Rust（代码内注释也承认是 follow-up）。本次会话时间范围内只做低风险修复；该问题已被记录为后续工作，S4 的 env 日志脱敏已降低实际泄露面。

- [x] **S3. CSP 形同虚设**
  - 位置：`src-tauri/tauri.conf.json`
  - ✅ 修复：收紧为最小 CSP——`script-src 'self' 'wasm-unsafe-eval'`（去掉 `'unsafe-inline'`），`connect-src 'self' ipc: http://ipc.localhost`（去掉 https/http/wss/ws 通配）。已确认前端无任何直接 fetch/WebSocket，index.html 无内联脚本，收紧安全。

- [x] **S4. API key 明文进日志**
  - 位置：`src-tauri/src/lib.rs:1789`
  - ✅ 修复：移除 `eprintln!("resolved_env: {:?}")` 明文打印，改为只打印 env 变量名集合（`resolved_env_keys`）。
  - ⏭️ 部分评估：AES-GCM 主密钥与密文同目录（`:882-973`）与 localStorage 翻译 key 明文存留在本次未改（涉及 Windows ACL 与前端存储架构，风险相对可控，记录为后续工作）。

- [x] **S5. 自动更新彻底失效**
  - 位置：`src-tauri/tauri.conf.json`、`editions/alpha/tauri.alpha.conf.json`
  - ✅ 修复：updater endpoint 从已删除的 `mistydew/tokenicode-deepseek-alpha/main/latest.json` 改为本分发仓库 `foreverbelief/my-claude-desktop/main/latest.json`（两处）。发布流程仍需在该仓库生成 latest.json。

---

## 二、高（影响使用体验与稳定性）

- [x] **H1. 会话切换竞态：异步重试/自动重启写错会话**
  - 位置：`src/hooks/useStreamProcessor.ts`、`src/components/chat/InputBar.tsx`
  - ✅ 修复：
    1. Provider 切换自动重试用流处理时已解析的 `tabId`（不再重读 `selectedSessionId`）
    2. Code 模式 ExitPlanMode 自动重启前校验 `selectedSessionId === tabId`，用户已切 tab 则中止自动重启并告警

- [x] **H2. 后台会话流错误写入前台会话**
  - 位置：`src/hooks/useStreamProcessor.ts` catch 块
  - ✅ 修复：catch 内通过 `msg.__stdinId → getTabForStdin` 解析真实归属 tab，错误消息写到正确会话

- [x] **H3. 删除运行中会话不清理进程/监听器/stdin 映射**
  - 位置：`src/components/conversations/ConversationList.tsx:390-406`
  - ✅ 修复：`executeDelete` 删除前先 `killSession` + 清理 `__claudeUnlisteners[stdinId]` + `unregisterStdinTab` + 清 meta.stdinId

- [x] **H4. 大文件附件 Array.from(bytes) 全量转 number[] 过 IPC**
  - 位置：`src/hooks/useFileAttachments.ts`
  - ✅ 修复：新增 20MB 附件大小上限，超限跳过并 console.warn（保留图片缩略图流程）

- [x] **H5. 拖拽监听器 unlisten 竞态 → 内存泄漏**
  - 位置：`src/hooks/useFileAttachments.ts:225-327`
  - ✅ 修复：cleanup 时若 promise 未 resolve 则链式 `unlistenPromise.then(fn => fn())`，消除泄漏窗口（含类型守卫）

- [x] **H6. flushStreamBuffer() 无参调用清空所有会话缓冲**
  - 位置：`src/hooks/useStreamProcessor.ts` clearPartial
  - ✅ 修复：改为 `flushStreamBuffer(msgStdinId || undefined)`，只刷当前会话 buffer

- [x] **H7. 流式高频直接写 store，重渲染风暴**
  - 位置：`src/hooks/useStreamProcessor.ts`、`src/stores/agentStore.ts`
  - ✅ 修复：
    1. `agentStore.updatePhase` 加同 phase + 同 currentTool 守卫（`agentStore.ts:47-55`）
    2. `lastProgressAt` 写入节流至 300ms（模块级 `_lastProgressByTab` Map）

---

## 三、中（稳定/体验/一致性问题）

- [ ] **M1. 下载无校验和 + zip-slip 解压 + 直接执行下载的安装器**
  - ⏭️ 已评估不修：涉及 Node.js/PortableGit 安装管线重构，属于高风险大改，本次会话不做；记录为后续工作。

- [ ] **M2. bypass 标志只在启动捕获，运行时切换失效**
  - ⏭️ 已评估不修：BypassModeMap 死代码（cargo 2 个 dead_code 警告即此）。结合 S1（默认不再 bypass）风险已降低；彻底接线需重构权限路径，记录为后续工作。

- [x] **M3. open_with_default_app cmd 元字符注入（Windows）**
  - 位置：`src-tauri/src/lib.rs`
  - ✅ 修复：改用 `tauri_plugin_opener::OpenerExt::open_path`（跨平台，无 shell 解释），删除 `cmd /C start` 分支

- [x] **M4. generate_session_title 用 --dangerously-skip-permissions**
  - 位置：`src-tauri/src/lib.rs:7694`
  - ✅ 修复：移除该 flag——标题生成是 `-p` 单次纯文本调用（--max-turns 1），不调用工具，无需权限跳过

- [ ] **M5. 版本体系脱节 / README 链接错乱**
  - ⏭️ 已评估不修：文档内容更新属于发布流程事项，不影响运行；已记录。

- [ ] **M6. 发布不可复现 + git 卫生**
  - ⏭️ 已评估不修：用户本地未提交改动不应由本次主动 commit；MSI/exe 清理属于发布仓库维护，已记录。

- [ ] **M7. CI 不可靠**
  - ⏭️ 已评估不修：CI 工作流改动无法本地验证，且非当前运行问题；已记录。

- [ ] **M8. 前端零测试**
  - ⏭️ 已评估不修：建立测试体系是独立工作项；已记录。

- [x] **M9. session-loader 多 text block 相同 id**
  - 位置：`src/lib/session-loader.ts:124-137`
  - ✅ 修复：同一 assistant 消息多 text block 使用 `msg.uuid_text_<blockIdx>` 生成唯一 id，避免 addMessage 去重覆盖

- [x] **M10. Windows 路径 projectDir 编码/解码不一致**
  - 位置：`src/stores/sessionStore.ts:148,166`
  - ✅ 修复：编码统一替换 `[\\/]` 两种分隔符 → Windows `D:\foo\bar` 编码为 `D:-foo-bar`，与 App.tsx 解码逻辑（`/^[A-Za-z]-/`）一致

- [x] **M11. localStorage 大 base64 无 try-catch**
  - 位置：`src/stores/sessionStore.ts`、`src/stores/settingsStore.ts`
  - ✅ 修复：
    1. `saveCustomPreviewsLocal` 包 try-catch 降级
    2. settingsStore persist 使用包装的 `safeStorage`（getItem/setItem/removeItem 全部 try-catch，超配额时 console.warn 不抛异常），经 `createJSONStorage(() => safeStorage)` 接入

- [x] **M12. pre-warm 失败监听器泄漏**
  - 位置：`src/components/chat/ChatPanel.tsx:1094-1097`
  - ✅ 修复：catch 块内清理已注册的 `__claudeUnlisteners[preWarmId]` 并删除
  - ⏭️ 部分：`__claudeStreamQueue` 无上限未改（正常路径下 handler 就绪后队列即清空，风险极低）

- [x] **M13. __claudeUnlisten 单例在多会话下互相覆盖**
  - 位置：`src/hooks/useStreamProcessor.ts`、`src/hooks/useRewind.ts`
  - ✅ 修复：删除全部 `__claudeUnlisten` 单例写入/清空；重试注册改为仅写入 `__claudeUnlisteners[retryId]`；process_exit 清理改为按 `exitingStdinId` 精准清理

- [x] **M14. fetchSessions 高频重复调用**
  - 位置：`src/hooks/useStreamProcessor.ts`
  - ✅ 修复：新增模块级 `debouncedFetchSessions()`（400ms 防抖），替换 5 处调用点；删除 1749-1750 的双重调用（含 1s setTimeout 冗余）

- [x] **M15. 自动 compact 15s 超时"假完成"**
  - 位置：`src/hooks/useStreamProcessor.ts`
  - ✅ 修复：超时不再置 idle/标记 completed；改为仅更新卡片 output 为「压缩仍在进行中…」（新增 i18n `chat.compactStillRunning`），真正结果仍由 result 事件回填

- [ ] **M16. entitlements 过松 / 死依赖**
  - ⏭️ 已评估不修：macOS 签名/entitlements 无 macOS 环境验证；已记录。

- [x] **M17. useActiveTab 不订阅 sessionStore**
  - 位置：`src/stores/chatStore.ts:292-298`
  - ✅ 修复：`useActiveTab` 内先订阅 `useSessionStore((s) => s.selectedSessionId)`，tab 切换自动重渲染，不依赖 key 重挂载

- [x] **M18. ensureTab 的 LRU 实为插入序淘汰**
  - 位置：`src/stores/chatStore.ts:557-577`
  - ✅ 修复：已存在 tab 访问时重新插入到 Map 末尾（MRU），淘汰顺序反映真实访问

- [ ] **M19. 调试日志过多**
  - ⏭️ 已评估不修：console.log 高频但多为关键路径诊断（startSession/切换），生产体验影响有限；已记录。

---

## 四、设置界面 API 提供商（用户指定，文字输出问题）

- [x] **U1. 设置 → API 提供商区域文字输出异常**
  - 位置：`src/components/settings/ProviderForm.tsx`
  - ✅ 修复：模型映射标签 `w-14`（56px）不足以容纳「DeepseekV4Pro」，改为 `w-32`（128px），文字溢出已解决

---

## 五、使用体验提升（用户要求"体验拉满"）

- [x] **X1. 默认模式与 bypass 切换体验**（配合 S1）
  - ✅ 完成：默认 code + bypass 二次确认 + 警示性标签（见 S1）

- [x] **X2. 模式切换/更新等操作增加即时反馈**
  - ✅ 完成：bypass 确认弹窗即为此类即时反馈；compact 超时提示（M15）避免"假完成"误导

- [x] **X3. 其他低风险体验优化**
  - ✅ 完成：H7 渲染性能（流式不再满帧重渲染）、M18 真 LRU（会话缓存更贴近实际使用）、M17 会话切换即时响应

---

## 修复顺序

按风险与影响排序：S1→S5 → H1→H7 → M3（注入）→ M1/M2 → M9-M19（按影响） → U1 → X1-X3

（本文件随修复进度更新状态标记）

---

## 最终验证记录（2026-08-17）

| 项目 | 结果 |
|---|---|
| 前端类型检查 + 构建 `pnpm build`（tsc && vite） | ✅ 通过（vite build 7s 完成，仅 chunk>500kB 警告） |
| Rust 后端 `cargo check` | ✅ 通过（仅 3 个 warning：BypassModeMap 死代码 + linker 消息） |
| Rust 完整 debug 构建 `cargo build` | ✅ `Finished dev profile` |
| 整体启动测试 `pnpm tauri dev`（60s） | ✅ 进程存活；应用窗口进程 mycode.exe 正常运行；Vite 1420 就绪；CLI 解析到 claude.cmd；网络检测正常；无崩溃/错误 |
| 测试残留清理 | ✅ mycode.exe / 日志文件已清理 |

### 遗留后续工作（本次已评估不修，风险已记录）
S2 文件白名单、S4 主密钥 ACL / localStorage key、M1 下载校验、M2 bypass 接线、M5-M8 文档/CI/测试、M16 entitlements、M19 日志清理
